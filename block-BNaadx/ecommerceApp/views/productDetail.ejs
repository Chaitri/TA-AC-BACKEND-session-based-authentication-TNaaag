<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Product | <%= product.name %></title>
  </head>
  <body>
    <header class="header flex">
      <a href="/">Home</a>
      <nav>
        <a href="/products">Products</a>
        <% if(!user.isAdmin) { %>
        <a href="/users/<%= user.id %>/cart">View Cart</a>
        <%} %>
        <a href="/users/logout">Logout</a>
      </nav>
    </header>
    <h1><%= product.name %></h1>
    <div class="articles">
      <div class="flex">
        <img
          src="<%= product.image %>"
          alt="<%= product.name %>"
          class="product-img"
        />
        <div class="article-item">
          <h3></h3>
          <h4>Price: ₹<%= product.price %></h4>
          <p>Quantity: <%= product.quantity %></p>
          <p>Likes: <%= product.likes ? product.likes : 0 %></p>
          <div class="btns">
            <% if(!user.isAdmin) { %>
            <a href="/products/<%= product.id %>/like" class="btn">Like 👍</a>
            <a href="/products/<%= product.id%>/dislike" class="btn"
              >Dislike 👎</a
            >
            <a href="/products/<%= product.id%>/cart" class="btn"
              >Add to Cart</a
            >
            <% } else if(user.isAdmin) { %>
            <a href="/products/<%= product.id %>/edit" class="btn">Edit</a>
            <a href="/products/<%= product.id %>/delete" class="btn">Delete</a>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
